var numbers, halved;

// Write your solution code below here using the variables declared above.

numbers = [1, 2, 3, 4, 5];

halved = numbers.map((num) => num / 2);

// Leave this portion of code alone to make testing work.

module.exports = { numbers, halved };
